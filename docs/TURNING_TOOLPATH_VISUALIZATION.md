# Turning toolpath visualization

PROFILE view is real: server-rendered X/Z SVG on light paper —
stock envelope, chain-line centerline, Z0 datum, mirrored finished
profile, selected segment with diameter/Z callouts, selected op's
toolpath (rapids dashed grey, cuts blue). Selection via the operation
table. 3D lathe view, playback scrubbing, stock-state animation and
cinematic turning: DEVELOPMENT, labelled in the workspace, not mocked.
