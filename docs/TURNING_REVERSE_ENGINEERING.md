# Turning reverse engineering — NOT BUILT (design notes)

Planned: photo intake (side/faces/thread/groove/bore close-ups), scale
reference or UNSCALED ESTIMATE, datums A (Z0 face) / B (centerline) /
C (locating feature), guided measurement missions reusing the
measurement-session model, thread intelligence (never inferring class
without evidence), and the nominal reasoning already live on the
workspace. The mill RE session flow is the pattern to extend.
