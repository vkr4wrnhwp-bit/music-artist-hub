# Route mapping — workspace consolidation (2026-08-11)

No route was deleted or renamed. Every URL that worked before the
consolidation still works; only the navigation surfacing changed. The
part drawer groups the fifteen part routes under five manufacturing
modes (collapsible; the group holding the current route opens):

| Mode | Routes (unchanged URLs) |
|---|---|
| PART | `/parts/[id]` (Overview) · `/proposals` · `/responsibility` · `/cost` |
| HOLD | `/setups` · `/soft-jaws` |
| CUT | `/tooling` · `/machinist` · `/nc-analyzer` |
| VERIFY | `/inspection` · `/fair` · `/readiness` |
| DELIVER | `/nc` · `/review` · `/tablet` |

Global rail sections are unchanged: Home, Parts, Machines, Tooling,
Workholding, Metrology, Jobs, Knowledge, Settings. (There is no
Analytics route in the product; the rail does not invent one.)

Deep links and bookmarks are unaffected — the pages did not move.
