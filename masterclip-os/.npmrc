# Third-party deps live at the workspace root and are hoisted so every package
# resolves the same copy. Workspace-internal edges are still explicit in each
# package.json.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
