"""Deterministic fixtures.

These exist so REACH can be exercised end to end without credentials and so the
adversarial tests have a stable corpus. A fixture result is always labelled as
a fixture in the UI and in job output — it is never presented as a live fetch.
"""
